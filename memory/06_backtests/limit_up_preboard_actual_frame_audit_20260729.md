# 板前候选实际帧审计（2026-07-29）

## Scope

- 审计窗口：`2026-07-20..2026-07-29`。
- 仅使用 PostgreSQL 已保存的盘中雷达帧和实时快照，不以收盘日线反推当时页面状态。
- 公开板前候选按产品当前精确定义：`near_limit`、涨幅至少 3%、A/B/C 触板就绪、尚未
  `actionable`、lane 验证通过、无阻断、快照新鲜，并限首板/二进三。

## Version boundary

- `2026-07-20..24` 保存的是 `limit-up-live-v15`，`2026-07-27` 先后是
  `limit-up-core-ab-v1` 和 `limit-up-core-abc-v1`；只有 `2026-07-28..29` 是当前
  `limit-up-core-abc-v2` 名称。
- 7 月 28--29 日的 v2 帧也跨了多个旧运行指纹。当前运行中的工作进程指纹与这些历史帧
  不同，因此历史帧只能证明当时页面实际展示了什么，不能替代修复后的自然前向证据。

## Actual result

| 日期 | 保存合同 | 实际板前候选 | 正式买点 | 说明 |
|---|---|---:|---:|---|
| 2026-07-20..24 | `limit-up-live-v15` | 0 | 有旧正式信号 | 不是当前 v2 合同，不能用于 v2 板前结论 |
| 2026-07-27 | `limit-up-core-ab-v1` / `limit-up-core-abc-v1` | 0 | 2 个旧信号 | 不是当前 v2 合同 |
| 2026-07-28 | `limit-up-core-abc-v2` | 0 | 0 | 当日无正式买点 |
| 2026-07-29 | `limit-up-core-abc-v2` | 0 | 1（巨人网络） | 正式信号只在触板后出现 |

因此，当前产品在这段可审计的实际帧中没有一次真正的“触板前板前候选”。此前把巨人网络
“提前约 22 分钟”的严格回放说成产品已展示，是错误口径；那是反事实计算，不是历史页面
曾经公开的候选。

## 巨人网络复核

- `002558.SZSE` 巨人网络在 `10:31:57` 已涨 `+6.604%`，直到 `10:51:58` 都有接近涨停
  的保存快照；实际首次封板和正式 `buy_now` 都是 `10:53:18`。
- 当时保存的 `10:31:57` v2 信号已经是 `qualified_waiting_trigger`，但
  `public_quality_touch_ready` 是空值而非真值；板前榜要求该字段严格为真，所以实际页面
  没有展示它。这是旧运行帧的直接事实。
- 用当前代码复算同一份未触板原始信号时，因没有 `signal_time` / `buy_time`，公共质量门会
  把空时间解析成 `00:00:00`，判为 `B_recognition_only_outside_entry_window`。只注入当前
  快照时刻后，结果变为 `qualified_waiting_trigger` 且 `touch_ready=true`。这说明旧帧的空值
  与当前代码的时间门缺口指向同一个根因：板前评估没有可用的假设触板时点。
- 只把当时快照时间作为“若此刻触板”的反事实时间输入，不改变任何质量字段，结果从
  `10:31:57` 开始满足板前条件，至真实触板提前约 `21 分 20 秒`。这证明缺口是时间字段
  传递，而不是巨人网络不符合 A/B/C 质量。

## Controlled counterfactual

在 `2026-07-28..29` 的 18,806 条已保存、接近涨停的 v2 信号上，仅补入当前快照时间后：

| 首次板前时刻 | 股票 | 涨幅 | 后续真实触板 / 正式买点 |
|---|---|---:|---|
| 2026-07-29 10:31:57 | 巨人网络 | +6.604% | 是 / 是 |
| 2026-07-29 13:07:54 | 华通线缆 | +5.511% | 否 / 否 |
| 2026-07-29 13:40:38 | 锡业股份 | +5.369% | 否 / 否 |

候选数量是 3 只、19 个快照事件，量级可控；但仅有巨人网络一只后续触板，不能将 `1/3`
当成触板率或收益结论，且 7 月 29 日的 D+1 尚未闭合。它只能证明时间修正会把所需的
观察候选及时暴露，而不会自动证明板前信号质量。

## Root Cause, Fix, And Verification

`core_quality.quality_entry_gate()` 只从 `buy_time` / `signal_time` / 已发生的触板时间读取
入场时点。普通未触板实时首板没有这些字段，时间门便读成 `00:00:00`；而板前榜又要求
`public_quality_touch_ready=True`。两者组合使本应观察的候选被排除。

修复已部署到本地 `alphaagent-api` 和实时扫描 worker：仅对“未触板、没有任何已观察到的
触板/买入时刻、且状态为 `near_limit`”的信号，在调用同一公共 A/B/C 质量门时传入当前
快照时刻作为临时评估输入。它不会写回 `signal_time`、`buy_time` 或
`trigger_observed`；真实触板后仍使用真实首触/回封时间重新运行相同质量门。

部署后用数据库中巨人网络的原始快照回放：`10:31:57` 得到板前候选，
`trigger_observed=false`、`buy_time=None`；触板后的保存帧得到正式可买，真实买入时刻来自
实际触板事件，且不再属于板前候选。新增回归覆盖 B 级 `10:30` 边界和早触未回封保护；相关
测试共 `167` 项通过。市场已收盘，修复后的自然页面帧仍须从下一交易日开始审计。
