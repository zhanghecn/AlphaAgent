# 首板动能买点 v13 前向起点（2026-07-17）

## Current State

- 实时版本：`limit-up-live-v13`；历史账本仍为 `limit-up-history-v15`，产品窗口仍为
  `limit-up-scheduled-v9`。
- 首板进入 5% 实时雷达后，不再要求距涨停不超过 1%。`lane_support_score >= 55`
  作为点时个股动能门，并继续要求战法结构、市场、行业或概念路径、资金、换手、历史
  风险和同股赚钱门全部通过。
- `near_limit / sealed / resealed` 使用同一动能入口。未封板触发价为当时现价；已封板
  或回封时现价即涨停价，信号只表示可以尝试排队，不表示已成交。
- 首板首次封板早于 10:00 不再永久否决；窗口内按当前评估时间重新检查动能和全部质量
  门。二进三仍使用窗口内真实首次触板或可观察回封，不随本次改动放宽。
- `missed_preseal_entry` 继续保留为诊断字段，但不再否决合格的首板动能信号。

## Point-in-time Evidence

- 本地 `limit_up_signal_snapshots` 只有最近 7 个日期键，`limit_up_live_trace_snapshots`
  只保留 2 个交易日。203 日一分钟库每日仅覆盖约 59-291 只定向股票，不是全市场
  5% 动能候选池；用最终触板股票反推动能买点会产生候选可得性未来函数，因此禁止。
- 旧快照按动能 55、完整板块路径和既有质量门重算得到 122 个逐次观察，集中在最近一日，
  当时均被成熟同路径历史风险门否决，且 D+1 尚未收盘，不能形成新胜率或收益结论。
- 最终镜像启动后的 `2026-07-17 10:54:55` 真实 v13 快照已落库，概念质量 `ready`、
  全市场概念覆盖 `99.967%`。4 个观察项均使用 `entry_kind=momentum`，其中 3 个封板票
  动能门通过。深南电A `000037.SZSE` 动能 `57.9234/55`、封板、战法资格通过；当前仅因
  实时炸板率 `37.5%` 超过市场门 `35%` 而保持 `approaching_trigger`，不是因涨幅位置或
  已经封板而被否决。

## Evidence Boundary

- v9 历史回放的 97 笔、`70.1031%` 胜率和 `+167.9810%` 复利来自触板成交代理，
  不能归属于 v13 提前动能买点。
- v13 从上线后的真实保存快照开始独立累计；必须按信号时现价、实际委托和成交回报评估。
  没有委托回报时，封板排队信号只能记为未确认成交。

## Verification

- 后端全部打板测试：`520 passed`。
- 前端：`72 passed`；TypeScript、生产构建、`compileall`、`git diff --check` 通过。
- 本地 API/Web 已重建，API、Gateway、PostgreSQL、Redis 健康；v13 快照持续落库。
