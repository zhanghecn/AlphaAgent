# Strategy Optimization Ledger

这个台账只保留当前仍有决策价值的策略结论。过程报告、截图、raw JSON、CSV 和长日志不作为长期记忆保留；需要复核时从持久化回测编号、API 和测试入口重新查。

## Current Product Baseline

- Public strategy: `mainline_dragon_pullback / 0.1.21`.
- Range: `2025-03-26..2026-06-18`.
- Execution model: `legacy_next_open`, signal visible at D close, fill at D+1 daily open.
- Candidate observation: top `100`, paged `20`.
- Portfolio execution fact: BUY candidates top `20`, max positions `10`; this remains real portfolio behavior, but it is no longer the main acceptance gate for candidate quality.
- Product default follows the historical high-return runs `#203/#194`: return about `+82.99%`, max drawdown about `-15.59%`, closed win rate `32.24%`, PF `1.6762`, buy/sell/open `224 / 214 / 10`.
- Portfolio backtests recompute candidates by default; persisted `quant_signal_runs` cache is only allowed when `reuse_signal_cache=true` is explicitly set for diagnostics/speed, and such runs are excluded from product baselines.
- Clean no-cache analysis run `#275` is explicitly `reuse_signal_cache=false`, return about `+48.80%`, max drawdown about `-23.83%`, closed win rate `29.76%`, PF `1.2420`, buy/sell/open `215 / 205 / 10`.
- `#275` does not improve both return and win rate versus `#203/#194`, so it must not replace the product default.
- Read-only attribution `GET /api/backtests/{id}/performance-attribution` now explains why a current run differs from a historical high-return comparable run. For `#275` the clean no-cache result is still far below historical `#203/#194`, but the comparison must be treated as lineage analysis, not a same-input parameter win/loss.
- `baseline_only=true` must return product-default, non-experiment runs protected by return and win-rate gates. A new clean/no-cache run may replace `#203/#194` only if it improves both return and win rate. Diagnostic run `#204`, all research-switch runs, and `reuse_signal_cache=true` runs are excluded from product baseline views.

## Product Decisions

- Keep one public strategy: `mainline_dragon_pullback`.
- Do not expose low-suction and dragon-pullback as ordinary user strategy switches.
- Market phase `主升 / 震荡 / 退潮 / 回暖` is audit/risk context only for now. It does not directly change score, rank, buy, sell, or position size.
- `低吸蓄势` is not automatically a daily BUY, but the default product does allow high-score, no-hard-risk low-suction buildup to remain executable. `low_suction_launch_confirmed=false` is a quality/stage label by default; it becomes a hard WATCH gate only when the explicit `require_low_suction_launch_confirmation` research switch is enabled. Confirmed low-suction launch, including tightly constrained strong-launch days after mature low-suction buildup, is now a small default score/ranking bonus only (`+1.2`, or `+1.6` for balanced first lift), not a buy-eligibility gate.
- `低吸首启` and `龙回头` are separate internal setup families. They share support/MA/volume/close-location features, but overlap does not auto-stack score.
- Stock detail markers are independent single-stock strategy replay markers: `买入 / 拒买 / 卖出`. They do not depend on portfolio capacity.
- Candidate independent trade quality report is read-only: it merges repeated BUY candidates into symbol clusters, simulates one theoretical D+1-open trade per cluster with current sell logic, and does not enter default signal score, buy/sell rules, portfolio sizing, or replacement decisions.
- Candidate independent trade quality uses a no-future entry rule: the trade entry is the first visible BUY in the cluster, not a later higher-score/confirmed signal. As of 2026-06-21, full rank20 candidate clusters (`#203/#275` source candidates) show about `2330` clusters, `2316` evaluated trades, `38.73%` win rate, `+1.81%` average return, `-5.86%` median return, `-7.92%` average max drawdown, and `+16.60%` average max runup.
- Candidate TopN quality must be read as candidate-pool quality, not a portfolio equity curve: Top10 means every historical signal day's ranks `1..10` are each simulated independently; Top20 means ranks `1..20` cumulatively. The separate rank-window table (`1-10 / 11-20 / 21-50 / 51-100`) shows marginal quality by daily rank band, and the signal-day table shows one trading day's Top10/Top20 outcomes.
- Unified strategy acceptance now uses no-position Top20 candidate quality as the main gate: same candidate date, current strategy Top20 vs product baseline Top20, each candidate independently bought at D+1 open and exited by current sell logic. Cash, max positions, full portfolio state, existing holdings and replacement are deliberately ignored.
- The 2026-06-22 full no-position Top20 baseline check used the unchanged strategy under the new acceptance lens: common candidate days `196`, current/baseline candidates `3820 / 3600`, average return `+1.86%` vs `+1.58%`, win rate `32.81%` vs `32.43%`, average max drawdown `-7.09%` vs `-6.91%`, worst drawdown `-34.13%` vs `-33.92%`. This is not a strategy update; it only shows that current Top20 return/win-rate are slightly better while drawdown quality is slightly worse.
- Candidate quality experiments must stay default-off until the matrix report and full acceptance both support promotion. The first candidate-tail-risk/mainline-momentum version is implemented but not promoted: quick matrix `2026-02-13..2026-06-18`, `max_symbols=120`, Top20 no-position showed tail-only only improved average return by about `+0.0020pp`, while momentum-only raised win rate by about `+0.54pp` but worsened average return by about `-0.1853pp` and average drawdown by about `-0.5364pp`.
- `enable_low_suction_buildup_quality_lane` is implemented as a default-off research switch, not a product-default update. It rewards clean low-suction buildup before launch confirmation; confirmed launch is only an extra bonus, and weekly-top-fractal relief is mild and only allowed for clean low-suction buildup. Full-market recompute for `002384.SZSE` on `2026-06-12` moved from default rank `6`, score `91.81`, to rank `5`, score `93.78` with the switch enabled. Recent-20-trading-day full-market Top20 matrix (`2026-05-22..2026-06-18`, `380` evaluated candidates) improved worst drawdown from `-34.1282%` to `-26.1443%`, but reduced win rate from `35.7895%` to `35.0000%` and average return from `+1.6784%` to `+1.5636%`. Decision: keep in the unified test channel only; do not promote.
- `enable_surge_quality_lane` is implemented as a default-off research switch, not a product-default update. It tries to separate active surge setups from crowded/stale Top20 candidates using only signal-day evidence. It failed the current matrix: quick `max_symbols=120` changed default `-2.3216% / 19.88% / -6.3531%` to `-2.3817% / 19.63% / -6.3789%`; expanded quick `max_symbols=500` changed default `-1.4597% / 24.79% / -7.0668%` to `-1.5402% / 24.47% / -7.1612%`. Decision: keep for audit only; do not promote.
- `enable_top20_day_quality_gate` is implemented as a default-off research switch, not a product-default update. It classifies the pre-ranked same-day Top20 as active low/mid acceptance or high-close weak-launch crowding, then reranks that same day's candidates using only signal-day visible evidence. Latest quick matrix `2026-02-24..2026-06-22`, `max_symbols=500`, failed: default `-1.3199% / 25.63% / DD -7.1669% / worst -34.1282%`; day gate `-1.4288% / 25.56% / DD -7.2347% / worst -34.1282%`. Attribution: it changed `35` candidate days, removed `67` default Top20 rows averaging `-0.3361%`, but added `67` rows averaging `-2.8664%`; it correctly removed many stale/quiet losers but also wrong-footed right-tail winners such as active high-close names. Decision: keep in the unified test channel only; do not promote.
- Expanded quick matrix `max_symbols=500` exposed a stronger but riskier opportunity class: tightened `enable_mainline_momentum_lane` materially improves no-position Top20 average return and win rate, but increases average and worst drawdown. After fixing matrix score-cache market-context consistency, latest `2026-02-24..2026-06-22` quick results were roughly: default `-1.37% / 24.76% / DD -7.08% / worst -34.13%`; `momentum_only` `-0.30% / 29.01% / DD -8.10% / worst -35.31%`; `tail_plus_momentum_risk_control` `-0.18% / 29.26% / DD -7.85% / worst -35.31%`; `momentum_risk_control_pure_loss_plus_mfe8_giveback` `-0.04% / 29.52% / DD -7.77% / worst -35.31%`. A narrowed hard-filter combination `tail_momentum_hard_pure_loss_plus_mfe8_giveback` restored worst drawdown to default (`-34.13%`) and kept return/win-rate better than default (`about -0.22% / 28.98%`), but average drawdown stayed worse (`about -7.45%`). Decision: mainline momentum plus profit protection is the strongest current alpha direction, but drawdown quality still fails promotion.
- Follow-up `mfe8_keep4_giveback6` variants improved the earlier profit-protection experiments but still failed promotion. Same window `2026-02-24..2026-06-22`, `max_symbols=500`: default `-1.3148% / 25.69% / DD -7.1613% / worst -34.1282%`; `mfe8_keep4_giveback6_stop` `-1.1368% / 27.55% / DD -6.9664%`; `momentum_risk_control_pure_loss_plus_mfe8_keep4_giveback6` `+0.0371% / 30.71% / DD -7.8020% / worst -35.3124%`; `tail_momentum_hard_pure_loss_plus_mfe8_keep4_giveback6` `-0.1138% / 29.87% / DD -7.4194% / worst -34.1282%`. Decision: profit protection helps returns and win rate, but the best momentum combo still worsens average drawdown, so it remains test-channel only.
- `trend_trailing_dd_buffer_pct` is a default-off sell research parameter. Default `0.0` preserves the current `trend_trailing_stop` thresholds; positive values relax the high-profit drawdown trigger. The 2026-06-28 sweep found `0.06` improved the in-sample full run, but the 3000-symbol CPCV check reported PBO `1.0` and baseline ranked best on all OOS paths. Decision: keep the parameter for reproducible research only; do not enable it in the product baseline or production defaults.
- The Top80 post-score test channel now includes two audit-only layers before touching the real strategy: signal-day-visible V2 reranking/weak-day compression and a probable unadjusted-price-discontinuity marker. The marker found that the worst raw losses are partly data-quality artifacts from unadjusted AkShare daily bars: default Top20 has `22/1557` suspected discontinuity paths averaging `-19.68%`, and excluding them changes default from `-1.3148% / 25.69% / DD -7.1613% / worst -34.1282%` to `-1.0516% / 25.99% / DD -6.9417% / worst -20.8241%`. V2 cap variants improve the test channel but are not product rules: latest `2026-02-24..2026-06-22`, `max_symbols=500`, `v2_cap10_overlay + mfe8_keep6_giveback5` reached raw `-0.0083% / 32.40% / DD -7.0093% / worst -32.8267%` with `963` candidates, while `v2_cap8_overlay + mfe8_keep6_giveback5` reached raw `+0.2057% / 33.14% / DD -7.0588% / worst -32.8267%` with `845` candidates; excluding discontinuity paths, cap8 is `+0.3884% / 33.33% / DD -6.9106% / worst -21.9124%`. Decision: this is the best current test-channel direction, but it cuts Top20 coverage materially and the raw worst drawdown still depends on data-quality correction. Do not promote or update the real strategy.
- The current-code factor path report is now the main explanation for why recent Top20 quality feels weak. Latest `2026-02-24..2026-06-22`, `max_symbols=500`, excluding suspected price discontinuities, has `1535` Top20 independent paths with average return `-1.0516%`, median `-4.4808%`, win rate `25.99%`, average max drawdown `-6.9417%`, and `34.92%` MFE/return `>=8%`. The problem is not absence of up-moves; it is too many pure-loss rows (`38.63%`) and too much MFE giveback (`13.03%`). Next candidate-layer test should distinguish support-lift low-suction/active low-mid acceptance from stale low-suction/high-close crowding/MA5 overextension, default-off only.
- The current-code launch path report explains the user's "买后先跌" concern more directly. In the same clean Top20 sample, only `34.20%` first hit `+5%` before `-3%`, while `60.85%` first hit `-3%` before `+5%` and `47.04%` first hit `-5%` before `+8%`. Weak visible signatures are MA5 overextension, tight quiet no activation, high-close crowded launch, confirmed-but-no-active-money, false-bull/warning without low support, and 6-10 day low suction without fresh lift. Low suction length itself is not a broad penalty: the fast-lift subset of stale-looking low suction still averaged strongly positive, so any rule must separate "fresh support lift" from "stale quiet".
- V3/V4/V5/V6/V7 postprocess follow-ups from that diagnosis improved default but did not beat V2 cap8. Same window `2026-02-24..2026-06-22`, `max_symbols=500`: `v3_support_lift_cap8 + mfe8_keep6_giveback5` clean `-0.0122% / 32.81% / DD -6.8211%`; `v4_launch_quality_cap8` clean `-0.2964% / 31.65% / DD -6.9156%`; `v5_v2_lift_guard_cap8` clean `+0.1852% / 33.05% / DD -6.9380%`; `v6_v2_right_tail_guard_cap8` clean `+0.3846% / 32.97% / DD -6.9534%` with only `746` candidates; `v7_v2_soft_right_tail_cap8` clean `+0.1843% / 32.97% / DD -6.9339%` with `845` candidates. V2 cap8 remains better on the combined return/win/drawdown/coverage tradeoff at `+0.3884% / 33.33% / DD -6.9106%` with `845` candidates. Decision: support-lift/MA5-turning and high-close right-tail exceptions are real, but score relief admits mediocre replacements while hard filters over-compress coverage. Do not promote; V2 cap8 remains the best test-channel direction.
- The postprocess stock audit explains V2's tradeoff. V2 removes bad buckets such as `other_confirmed_launch`, far-from-MA5, false-bull weak setups, and repeated launches; removed rows averaged `-2.1692%` with `25.00%` win rate. It also misses right-tail winners such as `000811.SZSE` 冰轮环境 `2026-04-16` and `000823.SZSE` 超声电子 `2026-05-06`, where high close was momentum confirmation under tradable MA5 distance and low warning, not crowding failure. Added winners tend to be active low/lower-mid support near MA5, while added losers often have warning `>=3` and MA5 slope negative/flat despite low close. Next rules should combine a narrow right-tail exception, stricter warning/MA5-slope rejection, and computed resistance distance; keep all default-off.
- 2026-06-25 latest current-code Top20 drilldown (`2026-02-26..2026-06-24`, `max_symbols=500`, `1554` evaluated rows, no suspected price discontinuities) confirms收益低 is mainly candidate-factor separation, not simply position count. Default Top20 current-sell path is `-1.4640%` average return, `24.71%` win rate, median `-4.4648%`, average DD `-7.1435%`, average MFE `+9.8865%`; `34.17%` still surge, but `39.45%` are pure losses and `13.32%` give back MFE. Path split: `up8_before_down3` has `+7.2481%` average return and `60.92%` win rate, but `38.79%` still give back to non-positive final return; `down5_before_up8` is `730/1554` rows, only `4.11%` win rate, `-5.8907%` average return, and `73.15%` pure loss. This is the core reason ordinary Top20 buy experience feels weak.
- 2026-06-25 factor comparison shows good buys are not merely "突破5日线". Good launch usually has signal-day controlled low/lower-mid or mid close, still near MA5/MA10, recent large-bull/limit-up activity, and fresh lift or tradable MA structure. Concrete good samples: `002384.SZSE` 东山精密 `2026-06-12` full-market recompute is BUY rank `10`, score `91.81`, 4-day low suction, low close `0.2773`, MA5 distance `+0.8848%`, MA10 distance `+0.5272%`, MA20 near flat, active strength `4`, recent limit-up/large-bull, entry `2026-06-15`, current path `+15.5796%` return, `+24.7809%` MFE, `-3.1825%` MAE. `000725.SZSE` 京东方A `2026-06-15` is BUY rank `13`, score `95.1727`, active strength `5`, near MA5/MA10, entry `2026-06-16`, path `+19.2243%` return and `+21.0793%` MFE. These are active controlled-lift cases, not simple MA5 breakout cases.
- 2026-06-25 bad-candidate buckets are narrower than "行情弱" or "低吸天数长". Worst repeatable structures are `other_confirmed_launch` with mid/high close and weak active money (`-4.8667%` average), `unconfirmed_buildup` at extreme high (`-4.2576%`), `repeated_launch` high close without right-tail guard (`-4.1531%`), MA5 overextension above `6%` (`-3.8462%`), `tight_quiet` with warning `>=3` (`-3.2377%`), false-bull warning `>=3` without support (`-2.8316%`), and stale `6-10` day low suction without clean lift (`-2.2214%`). Concrete bad samples: `001207.SZSE` `2026-03-30` was rank `3` but extreme high close, MA5 distance `+4.15%`, MA10 `+6.20%`, warning `3`, then `-32.2642%`; `001229.SZSE` `2026-06-04` was other-confirmed, tight MA, active strength `1`, false-bull/warning `3`, then `-27.2416%`.
- V8 strict bad-bucket postprocess is the current best test-channel direction but still not promotable. Same latest window with `mfe8_keep6_giveback5`: default Top20 is `-1.2723% / 27.67% / DD -6.9355% / worst -34.1282%`; V2 cap8 is `-0.1627% / 32.07% / DD -7.1418% / worst -32.8267%`; V8 cap8 is `+0.3638% / 33.16% / DD -6.9513% / worst -32.8267%`, with `752` candidates. V8 removed `962` default rows averaging `-2.3124%` and added `157` rows averaging `+0.1934%`, so it improves quality by removing first-down buckets. It still compresses weak days to cap8 and can miss rank-lower right-tail winners such as `000811.SZSE` `2026-04-16`: that stock was not blocked by V8 strict notes and had a valid right-tail guard, but weak-day cap8 pushed it out from default rank `20`; the next experiment should reserve a narrow right-tail slot or score boost under cap8, not simply loosen all high-close penalties.
- Candidate quality matrix market-context consistency was fixed on 2026-06-22: all default-off scoring experiments that read `dynamic_market_regime`, `market_warning_level`, `recovery_state`, or breadth now trigger market-context backfill even when daily scores come from a cache. This changed the reliability of tail/momentum/surge comparisons; older quick matrix numbers that used `unknown/w0` for some variants are historical only.
- Fixed holding-path diagnosis is now separate from sell-logic candidate quality. Persisted `0.1.21` Top20 rows (`3600`, evaluated `3580`) show D+1 execution-day close win rate `48.72%`, second holding-day close win rate `48.52%`, hold-20 average return `+3.1133%`, but hold-20 median return `-1.3572%` and average hold-20 MAE `-10.3857%`. This confirms the Top20 pool is right-tail driven: average return can be positive while ordinary candidates still lose and experience large drawdown.
- Portfolio backtest remains the real execution truth: BUY signal still must pass D+1 open, cash, max-position, limit-up/limit-down, rank, and replacement constraints.
- Portfolio backtest reproducibility rule: default runs must not read historical candidate caches. Cache reuse is an explicit diagnostic mode only and is marked by `assumptions.signal_cache_reuse`.
- Product promotion rule: do not select a new strategy/run as default unless both return and win rate improve over the current product default.

## Current Diagnosis

- Candidate quality has measurable positive edge after removing position constraints: full rank20 independent candidate trades average about `+1.81%` with `38.73%` win rate, but the median trade remains negative. The larger loss source is still the execution chain: full positions, replacement quality, sell timing, and protection of existing trend winners.
- Max positions `10` is not the main bottleneck. The post-fix 2026-06-21 sensitivity grid found:
  - Capital-neutral sizing: `5 x 20%` `+59.09%` / DD `-26.00%`; `10 x 10%` `+45.17%` / DD `-23.83%`; `15 x 6.67%` `+45.67%` / DD `-21.92%`; `20 x 5%` `+29.47%` / DD `-18.62%`.
  - Slot-only with fixed `10%`: `5` slots `+29.22%`; `10` slots `+45.17%`; `15` slots `+65.93%` with `89` insufficient-cash rejects; `20` slots `+45.17%` with `488` insufficient-cash rejects.
- Interpretation: wider portfolios dilute the candidate edge; more concentrated portfolios can lift return but increase drawdown. Fixed `10%` with more than `10` slots is a cash-constraint experiment, not a clean capacity improvement.
- Baseline reproducibility was tightened before further parameter grids. Default portfolio backtests no longer read persisted score caches; explicit score-cache reuse now requires the current `signal_evidence_schema_version`; old `#203/#194` and current-schema results should not be mixed in comparisons.
- The apparent return/win-rate drop from `#203/#194` to `#275` is mainly a baseline lineage problem, not a max-position effect: old high-return runs used older candidate-cache semantics, while `#275` recomputes candidates from current code and data. Silent cache reuse was unsafe because it let a current backtest consume historical candidate inputs whose generation code and evidence semantics were different. Product default still follows `#203/#194` until a new run beats both return and win rate.
- The measured drop is mostly reduced winner contribution: `#274` gross wins are about `37.76` 万 lower while gross losses are only about `1.03` 万 worse. Trend trailing winners dropped from `33` to `24` closed trades and contributed about `34.98` 万 less. This points to winner admission/protection and replacement path quality, not just raw win-rate chasing.
- The 2026-06-22 stock-level audit for clean no-cache `#275` vs product baseline `#203` separates two layers. First, historical no-position Top10 cohort testing removed cash, max-position, full-portfolio and existing-holding constraints and showed the current candidate layer slightly weaker than baseline. Current acceptance has since been upgraded to no-position Top20. Second, real portfolio attribution found that many missed baseline winners were still visible in the current same-day candidate list, but the real portfolio was already `10/10` full and did not trigger slot reallocation. Next work should drill down no-position Top20 factors first; winner admission and slot reallocation are separate portfolio diagnostics.
- Latest Top20 result changes the immediate optimization target: current candidates already beat baseline slightly on average return and win rate, so the next candidate-layer work should identify the tail-risk and average-drawdown buckets inside Top20 rather than removing or expanding portfolio slots.
- The 2026-06-22 candidate loss and missed-winner audit found that Top20 candidate losses concentrate in `dragon_pullback + high_close_launch`, `choppy_rotation + unconfirmed/thin/high-close launch`, and high-close dragon pullbacks with medium/wide MA dispersion. Refined 20-day winner scanning also found a different opportunity class: recent-limit-up/large-bull active names that are either outside Top20 or absent from the current low-suction/dragon-pullback candidate system. Treat this as input for default-off risk-tail demotion and a separate mainline momentum lane; do not broaden ordinary low-suction buy frequency.
- The 2026-06-22 fixed holding-path audit explains the user's "second day win rate is low and long holding still loses" concern. Top20 candidate ranks degrade materially from `1-5` to `16-20`; rank `1-5` hold-2 win rate is `50.65%`, while rank `16-20` is only `45.83%`. Weak fixed-path buckets include `thin_volume_launch + high close`, `high_close_launch + normal volume`, `repeated_launch + high close`, `unconfirmed_buildup + normal volume`, and weak/choppy high-close launch combinations. Next experiments should test default-off weak-bucket demotion or delayed D+2 follow-through entry, and must compare missed right-tail winners against avoided losers.
- The 2026-06-22 factor surge/decline audit for `#275` Top20 shows the same issue at factor level: raw Top20 average 10D return is `+2.30%`, but median is `-0.46%` and `59.26%` hit loss first. Surge comes mostly from active mainline/dragon traits such as recent limit-up, `large_bull_count_20d >= 3`, wide MA trend structure, and lower-mid close. Decline comes from high-close weak launches, stale `low_suction_days 6-10` without fresh activation, unknown setup family, and same-day broad Top20 failure. Next candidate-layer experiment should be a narrower default-off surge-quality lane plus weak-launch/high-close/stale-low-suction demotion, not a broad low-suction or momentum boost.
- A stricter executable-Top20 recheck of `#275` (`rank 1..20`, `entry_action=BUY`, `executable_entry_signal=true`, `3580` rows) confirms the short-hold problem: D+1/D+2/D+3 close win rates are only `48.50% / 48.45% / 48.81%`; 10D average return is `+1.98%` but median is `-0.60%`; `23.88%` are pure losses (`MFE<3%` and 10D return<0), `12.01%` give `>=8%` MFE then finish non-positive, and `59.47%` hit `-3%` before `+5%`. Best short-path bucket is active money + mid/lower close + wide MA; worst short-path buckets are high-close weak launch, stale low-suction without active money, and too-tight quiet structures. This points to two separate default-off tests: delayed/no-future follow-through entry for pure losers, and targeted profit protection for MFE giveback.
- A follow-up strict drilldown removed old `rank=0` artifacts and merged same-stock consecutive repeats. Cluster-first Top20 still had average 10D `+1.66%`, median `-0.73%`, win rate `47.16%`, pure-loss `23.90%`, and MFE>=8 giveback `12.19%`, so the weak ordinary-trade experience is not only a duplicate-winner artifact. Visible good condition: active money + low/lower-mid close + MA `3..18` + near MA5 (`534` rows) had `+3.22%` average 10D and `51.31%` win rate. Visible bad conditions: high-close weak launch (`741` rows) had only `+0.45%` average 10D and `43.99%` win rate; stale low-suction without active money (`471` rows) was `-0.13% / 43.10%`; tight quiet without active money (`364` rows) was `+0.08% / 43.13%`. Day-level composition matters, but the first default-off Top20 day-quality reranker failed because it was too coarse and replaced weaker default names with even weaker outsiders. Future day-context work must be narrower than whole-day reranking and must explicitly protect right-tail active names.
- The current-sell no-position quick path (`2026-02-13..2026-06-18`, `max_symbols=500`) explains why收益/胜率仍低 even when average MFE is high. Default Top20 average final return was `-1.4597%` with average MFE `+9.9975%`; `13.68%` of candidates had MFE `>=8%` but still finished non-positive, while `39.11%` never reached `+3%` MFE and finished negative. Momentum improved winner size (winners average `+16.32%`) but increased both giveback (`17.51%`) and pure-loss drawdown. A default-off high-risk D+2 follow-through entry test was added to the unified test channel and failed the same `max_symbols=500` matrix: default `-1.4597% / 24.79% / DD -7.0668%`, D+2 follow-through `-1.7868% / 23.97% / DD -7.5239%`, evaluated candidates `1210 / 1557`. It filtered weak names but delayed accepted high-risk names into worse entries, so do not promote delayed confirmation as a broad entry fix.
- The newest fixed-path drilldown shows the factor split more clearly. In the `2026-02-13..2026-06-18`, `max_symbols=500` Top20 set, fixed 10-day return was `-1.31%` average, `-2.82%` median, and only `38.34%` win rate. The strongest bucket was `active>=3 + close<0.35 + MA 3-6`, with `+10.86%` average 10D and `50.00%` win rate; `active>=3 + close 0.58-0.75 + MA 10-14` had high MFE (`72.73%` reached MFE>=8%) but current-sell final average was still negative. Worst buckets include `thin_volume_launch + high close`, `other_confirmed_launch + mid-high close`, `repeated_launch + high close`, and unconfirmed high/mid-high close. Next work should split two default-off tests: signal-day weak-bucket demotion for pure losers, and profit-protection for high-MFE giveback.
- Current sell-logic candidate quality does not match fixed-horizon labels perfectly. In the latest quick windows, default Top20 still has many negative candidate days; weak same-day cohorts include broad `unconfirmed_buildup`, stale low-suction without active money, and high-close weak-launch buckets. Ranking tweaks alone are unlikely to create a large win-rate jump when the whole candidate day is bad. A broad D+2 follow-through entry was tested and failed, so any day-risk gate must be narrower than “wait for confirmation”.
- Latest current-sell audit of the momentum/hard-filter path found the main residual drag among added candidates: active mainline names in `false_bull`/warning contexts with extreme MA dispersion, and risk-day names far above MA5. Added-candidate counterfactuals showed simple broad filters such as `warning>=3 + ma5_distance>3.5` remove positive-return buckets, while narrower `warning>=3 + ma5_distance>8` and `false_bull + warning>=2 + MA>=18` are safer hard-filter candidates. These were added only to the default-off hard-filter experiment, not to the real strategy.
- The 2026-06-22 follow-up analysis used the unified no-position Top20 channel on `2026-02-24..2026-06-22`, `max_symbols=500`, and confirmed the current issue is not lack of upside. Default Top20 had average final return `-1.3148%`, win rate `25.69%`, average DD `-7.1613%`, but average MFE `+10.0565%`; `38.41%` were pure-loss paths and `13.55%` reached MFE `>=8%` then finished non-positive. `momentum_risk_control + pure_loss + mfe8_giveback` improved return/win to `-0.0279% / 29.89%`, but worsened average/worst DD to `-7.8347% / -35.3124%`. The hard-filter combo recovered worst DD to `-34.1282%` and kept return/win better than default (`-0.1831% / 28.98%`), but average DD was still weaker at `-7.4590%`. Best visible surge buckets were active low/lower-mid close near MA (`active3-4 + close<0.35 + MA3-6`: `+9.4967%`, win `47.83%`; `active1-2 + lower-mid + MA3-6`: `+3.2764%`, win `45.16%`). Bad buckets were active/extreme-close MA `6-10`, `other_confirmed_launch + mid-high close`, `high_close_launch + high/extreme close`, stale low-suction without active money, and high-MFE giveback with far-from-MA5 active momentum. Re-ranking counterfactuals with same-day Top20 backfill showed only modest improvement (`combined_v2` about `+0.0781% / 30.08% / DD -7.6687%`; `combined_v3` about `+0.0319% / 29.70% / DD -7.5668%`), still failing the drawdown gate. Decision: continue research in default-off test channel; do not update the real strategy.
- The same latest quick channel shows why "buy and wait" still feels weak. From D+1 open, fixed close-horizon results were: day 1 average `+0.1835%`, win `49.26%`; day 2 average `-0.0119%`, win `46.06%`; day 5 average `-0.5216%`, win `42.32%`; day 10 average `-1.3009%`, win `38.72%`. The sell-logic path has high average MFE, but ordinary candidates turn negative quickly unless they enter the right-tail surge group. This supports separate default-off work on pure-loss avoidance and high-MFE profit protection instead of simply holding longer.
- Market context matters, but not as a blunt filter. In the latest context-filled quick drilldown, weakest broad buckets were `false_bull/w3` (`-3.5139%`, win `17.00%`), `choppy_rotation/w2` (`-3.5257%`, win `15.00%`), and `false_bull/w2` (`-2.3222%`, win `24.44%`). `false_bull/w4` had a small sample but positive result, and `choppy_rotation/w0/w3` contained both best and worst days. Conclusion: use market context only with setup quality, close location, MA distance and active-money interaction; do not hard-filter all `false_bull` or all warning days.
- Current factor evidence supports this working rule: 猛拉 usually requires active-money evidence plus a still-tradable low/lower-mid or controlled mid close, enough MA spread to retain trend energy, and distance from MA5 that is not already excessive. Buy-after-drop paths cluster in high-close weak launches, stale 6-10 day low-suction without active money, tight quiet structures without activation, and candidate days where Top20 itself is crowded in active high-close weak-launch names. MA5 breakthrough is a useful visual description of startup, but not a sufficient rule by itself; the score needs to separate "gradual lift from support" from "high close after consensus."
- Current launch-path evidence refines that working rule: the first priority is reducing rows that hit `-3/-5` before any tradable `+5/+8` lift. Do not simply add a broad low-suction reward or broad low-suction penalty. Reward low/mid support lift only when MA5/MA10 remain tradable and there is fresh lift or active money; demote high-close crowding, MA5 distance `>6%`, tight quiet no activation, confirmed-without-active-money, and weak setup under false-bull/warning conditions.
- `002384.SZSE` on 2026-06-12 is a concrete regression/audit case for candidate ranking semantics. Current code still produces a BUY low-suction buildup signal, but it no longer ranks as historical TOP1 because `candidate_lanes.py` sorts the dragon execution pool by opportunity score, giving mature/confirmed low-suction names a much larger bonus than a 4-day unconfirmed buildup. Persisted `0.1.21` run `#5772` is also inconsistent with current semantics: the signal row is BUY, while the recommendation row is missing because the run was produced when unconfirmed low-suction still failed the launch-confirmation gate. Do not use that persisted recommendation table alone to diagnose current candidate quality.
- `000725.SZSE` on 2026-06-15/16 is a UI/date-mapping and weekly-factor-stability audit case. The 2026-06-16 single-stock buy marker is a D+1 execution; its candidate signal date is 2026-06-15. After changing weekly aggregation to ISO calendar weeks, current full-market recomputation has the 2026-06-15 BUY candidate around rank `13`, score `95.1727`, without `weekly_top_fractal_risk`. The stock detail page should show candidate status by marker signal date, not only by latest candidate or execution date.
- `weekly_top_fractal_risk` is not a proven bad-candidate label, but its original daily-to-weekly aggregation was unstable because it grouped every 5 bars from the loaded lookback start. The 2026-06-22 audit found persisted `0.1.21` Top20 flagged candidates had comparable or better quality than unflagged candidates (`36.36%` win rate and `+1.5916%` average return vs `32.25%` and `+1.5755%`), while counterfactual Top20 reranking by adding back `+2/+4` points improved average return/win rate but worsened average drawdown to about `-7.07%`. The implementation now groups weekly bars by ISO calendar week; `enable_weekly_top_fractal_relief` remains default-off and did not improve the quick Top20 matrix beyond the calendar-week fix.
- Current local market context on `2026-06-18` is `假强势`: index trend/momentum are strong but breadth is weak and fund flow is `panic_outflow` with market warning level `4`. In this context, unconfirmed low-suction and high-close weak-volume launches need a narrower default-off risk gate; do not broaden buy frequency.
- Phase reports no longer treat current-schema signal events as all unknown: nested `raw.evidence` is expanded and missing rows get visible market context annotation. For `#274`, real trades group roughly as `震荡 106 / 退潮 86 / 主升 17`; `#275` should be the next phase-distribution reference after runtime report refresh.
- `support_stop` is not one sell bug. It mixes failed launch, sold-before-rebound, float-profit giveback, and bad replacement after a sell.
- Broad fixes have repeatedly failed: wide low-suction score boosts, hard low-suction gates, simple market weighting, failed-launch early exits, high-score rotation, weak-holding rotation, and protected weak-holding rotation all stayed below the baseline. The default confirmed-launch bonus is deliberately narrow and small; it should be retested before any larger low-suction weighting is promoted.
- The next useful work should be narrow and default-off: execution consistency, trend-winner protection, replacement quality, and no-future single-stock marker correctness.

## Major Experiments

| Run | Tested | Result | Decision |
| --- | --- | --- | --- |
| `#203/#194` | Historical high-return default strategy lineage. | `+82.99%`, max DD `-15.59%`, `224 / 214 / 10`. | Current product default while analysis continues. |
| `#275` | Clean no-cache product parameters. | `+48.80%`, max DD `-23.83%`, `215 / 205 / 10`. | Analysis baseline only; not promoted because return and win rate are lower. |
| `#274` | Current-schema cached reference before the cache boundary was tightened. | `+45.17%`, max DD `-23.83%`, `219 / 209 / 10`. | Historical/diagnostic only; not current baseline. |
| `#255..#258` | Low-suction-only, dragon-only, phase-aware selector, phase selector plus replacement quality. | Returns `+15.87% / +38.56% / +45.52% / +39.19%`; all below baseline. | Rejected. Splitting strategy families or adding phase selector did not improve the portfolio path. |
| `#259/#260` | Low-suction MA10 pullback entry with and without slot reservation. | `+43.63%` and `+23.09%`; both worse drawdown than baseline. | Rejected. Pullback wait semantics need narrower confirmation and better execution handling. |
| `#261` | Low-suction trigger-day confirmation, buy next open. | `+46.81%`, max DD `-24.47%`. | Rejected. Candidate-level confirmation has value, but current sell/replacement path loses too much. |
| `#262/#263/#264` | Low-suction branch exits, replacement quality gate, strict replacement setup gate. | Best variant `#264` about `+55.25%`, still far below baseline. | Rejected. The direction reduces some weak replacements but misses trend payoff. |
| `#265/#267` | Dynamic failed-launch exit and replacement-quality gate. | `+32.56%` and `+34.76%`, both worse DD than baseline. | Rejected. Early exits release slots into worse replacements and miss winners. |
| `#268` | High-quality trend candidate rotation when full. | `+42.40%`, max DD `-21.97%`. | Rejected. Broad high-score rotation disrupts trend winners. |
| `#270/#271` | Weak-holding quality rotation and protected weak-holding rotation. | `+39.63%` and `+42.28%`, max DD `-22.55%`. | Rejected. D+1 weak holdings often repair by open; tradable replacement set is too small and weak. |
| Top20 acceptance 2026-06-22 | Full no-position current Top20 vs product baseline Top20, with no strategy change. | Return `+1.86%` vs `+1.58%` and win rate `32.81%` vs `32.43%` are better, but average DD `-7.09%` vs `-6.91%` and worst DD `-34.13%` vs `-33.92%` are worse. | Baseline check only. Use it to target Top20 drawdown quality before proposing any strategy change. |
| stock audit 2026-06-22 | Specific missed winners and added losers in `#275` vs `#203`. | Missed winners are mostly visible high-rank candidates but blocked by full portfolio/no slot reallocation; added losers are high-score failed launches in weak/risk contexts. Historical Top10 candidate cohort was slightly weak; current acceptance uses Top20. | Use as design input for no-position Top20 drawdown drilldown plus separate default-off portfolio experiments; not a strategy promotion. |
| candidate loss / missed winner audit 2026-06-22 | Top20 independent candidate losers and refined 20-day whole-market winners. | Candidate edge is right-tail driven: average `+1.8149%`, median `-5.8553%`, win rate `38.73%`. Bad buckets are high-close/choppy/unconfirmed launch combinations; missed winners often have recent limit-up/large-bull momentum and do not fit the current pullback/reclaim lane. | Use for default-off Top20 risk-tail penalty and mainline momentum lane tests. |
| weekly factor stability / candidate experiments 2026-06-22 | Fix weekly aggregation to ISO calendar weeks; keep default-off `enable_weekly_top_fractal_relief`, `enable_candidate_tail_risk_penalty`, and `enable_mainline_momentum_lane` in unified test channel. | Quick matrix after calendar-week fix: default `-2.3216% / 19.88% / -6.3531%`; weekly_relief unchanged; tail-only `-2.3122% / 19.88% / -6.3362%`; momentum-only `-2.4930% / 20.57% / -6.8788%`; tail+momentum `-2.4895% / 20.57% / -6.8728%`. | Keep calendar-week fix. Do not promote weekly relief, tail-only, or momentum lane; improvements are too small or worsen drawdown. |
| low-suction buildup quality lane 2026-06-22 | Default-off `enable_low_suction_buildup_quality_lane`: clean low-suction buildup gets a small ranking bonus before launch confirmation; launch confirmation is extra only. | `002384.SZSE` `2026-06-12` moved default rank `6` -> experiment rank `5`; recent-20-day full-market Top20 matrix improved worst drawdown by about `+7.98pp` but reduced win rate by `-0.79pp` and average return by `-0.115pp`. | Keep as test-channel experiment only. It fixes the East Mountain Precision style ranking case without enough whole-pool improvement to update the real strategy. |
| fixed holding path audit 2026-06-22 | Persisted `0.1.21` Top20 bought at D+1 open and measured at fixed close horizons. | Entry-day close win rate `48.72%`; hold-2 close `48.52%`; hold-20 avg `+3.1133%` but median `-1.3572%`, average MAE `-10.3857%`. | Confirms poor ordinary-trade experience despite right-tail average gains. Use for weak-bucket demotion or delayed confirmation experiments; not a strategy promotion. |
| factor surge/decline audit 2026-06-22 | `#275` Top20 factor snapshots joined to fixed-horizon outcomes; raw Top20 and cluster-first views. | Raw Top20 avg 10D `+2.30%`, median `-0.46%`, win `47.88%`, loss-first `59.26%`; cluster-first avg 10D `+1.69%`, median `-0.73%`. Strong buckets are active recent-limit-up/large-bull plus lower-mid close or balanced 3-5 day low-suction; weak buckets are high-close launch, stale 6-10 day low-suction without activation, unknown family, and same-day Top20 collapse. | Use as design input for a narrower default-off surge-quality plus weak-launch demotion test; no product strategy change yet. |
| current-code factor path 2026-06-22 | Current code Top20 from Top80 context pool, no-position D+1 open path, clean view excluding suspected price discontinuities. | `1535` evaluated; average `-1.0516%`, median `-4.4808%`, win `25.99%`, average DD `-6.9417%`, surge rate `34.92%`, pure-loss `38.63%`, giveback `13.03%`. | Do not promote. Next default-off test should reward active low/lower-mid support lift and demote inactive stale low-suction, high-close/repeated launches, and MA5 overextension as interactions, not as broad filters. |
| current-code launch path 2026-06-22 | Current Top20 launch ordering from D+1 open: whether the path first reaches `+5/+8` or first breaks `-3/-5`, using only signal-day evidence for cause buckets. | Clean `1535` evaluated; first `+5` before `-3` `34.20%`, first `+8` before `-3` `23.26%`, first `-3` before `+5` `60.85%`, first `-5` before `+8` `47.04%`. `up8_before_down3` averaged `+7.78%`, while `down5_before_up8` averaged `-5.59%`. | Do not promote. Use this as V4 design input: reward MA5-turning/support-lift low/mid entries, but demote MA5 overextension, tight quiet no activation, high-close crowded launches, confirmed-without-active-money, and warning/false-bull only as weak-setup interactions. |
| V3 support-lift postprocess 2026-06-22 | Default-off Top80 postprocess using support-lift reward plus stale/crowded/MA5-overextension demotion, with MFE keep6 sell. | V3 cap8 clean `-0.0122% / 32.81% / DD -6.8211% / worst -21.9124%`; better than default clean `-1.0516% / 25.99% / DD -6.9417%`, but weaker than V2 cap8 clean `+0.3884% / 33.33% / DD -6.9106%`. | Not promoted. Keep as evidence; V2 cap8 remains the stronger test-channel direction, and both still require data-quality/full-market work before strategy update. |
| V4/V5 launch-quality postprocess 2026-06-22 | Default-off Top80 postprocess variants after launch-path diagnosis. V4 rewarded support-lift/MA5-turning and demoted first-down signatures; V5 used V2 strict filtering plus a narrower lift guard. | Formal quick clean: default `-1.0516% / 25.99% / DD -6.9417%`; V4 cap8 `-0.2964% / 31.65% / DD -6.9156%`; V5 cap8 `+0.1852% / 33.05% / DD -6.9380%`; V2 cap8 remains best at `+0.3884% / 33.33% / DD -6.9106%`. | Not promoted. V4/V5 confirm that low/lower-mid MA5-turning support lift matters, but the guard must be narrower; current收益低 is still mostly first-down/pure-loss candidates plus MFE giveback. |
| postprocess stock audit + V6/V7 2026-06-22 | Stock-level drilldown of V2/V5 removals/additions with signal factors, MA, pressure, market warning, and entry path; V6 tested narrow high-close right-tail exception plus weak-warning rejection; V7 kept V2 coverage and tried soft score relief. | V2 removed rows avg `-2.1692%`, added rows avg `+0.2114%`; V6 cap8 clean `+0.3846% / 32.97% / DD -6.9534%`, count `746`; V7 cap8 clean `+0.1843% / 32.97% / DD -6.9339%`, count `845`, with added rows only `+0.0573%`. | Not promoted. Broad score relief is too noisy; hard right-tail protection compresses coverage. Next useful work is candidate Top80 snapshot/cache plus a more precise stock-level exception design before V8. |
| V8 strict bad-bucket postprocess 2026-06-25 | Default-off Top80 postprocess after detailed current-code path drilldown. It filters narrow first-down buckets and uses `mfe8_keep6_giveback5`; no real strategy change. | Latest `2026-02-26..2026-06-24`, `max_symbols=500`: default+mfe `-1.2723% / 27.67% / DD -6.9355% / worst -34.1282%`; V2 cap8 `-0.1627% / 32.07% / DD -7.1418%`; V8 cap8 `+0.3638% / 33.16% / DD -6.9513% / worst -32.8267%`, candidates `752`. Removed rows averaged `-2.3124%`, added rows averaged `+0.1934%`. | Keep test-channel only. It improves candidate quality by removing first-down buckets, but weak-day cap8 still misses rank-lower right-tail winners. Next test should add a narrow right-tail reserve/boost before any promotion. |
| high-risk D+2 follow-through 2026-06-22 | Default-off `enable_high_risk_d2_follow_through_entry` in the unified candidate-quality test channel only; high-risk candidates require D+1 close confirmation and enter at D+2 open. | Quick `max_symbols=500`: default `-1.4597% / 24.79% / -7.0668% / worst -34.1282%`; D+2 follow-through `-1.7868% / 23.97% / -7.5239% / worst -34.4189%`, evaluated `1210 / 1557`. Rejected high-risk names were weak (`-2.35%` default path), but accepted delayed names worsened from `-1.65%` default path to `-3.18%` delayed path. | Rejected for promotion. Keep default-off as evidence; delayed confirmation is not a broad fix. |
| surge-quality / momentum / momentum-risk matrix 2026-06-22 | Default-off `enable_surge_quality_lane`, `enable_mainline_momentum_lane`, `enable_mainline_momentum_risk_control`, `enable_mainline_momentum_hard_filter`; unified no-position Top20 matrix. | `surge_quality` failed. Expanded `500` momentum improved average return by `+1.06pp` and win rate by `+3.95pp`, but worsened average/worst drawdown by `-1.04pp / -1.18pp`. `tail_plus_momentum_risk_control` improved return/win rate the most (`-0.2998% / 29.06%`) but still worsened average/worst drawdown; `tail_plus_momentum_hard_filter` restored worst drawdown to default and improved return/win rate versus default, but average drawdown still worsened. | Do not promote. Next test should avoid broad hard filters; use signal-day weak-bucket demotion and separate profit-protection tests instead of broad delayed confirmation. |
| Top20 day-quality gate 2026-06-22 | Default-off `enable_top20_day_quality_gate`: classify same-day pre-ranked Top20 composition, then rerank candidates on good/bad day structure. | Quick `2026-02-24..2026-06-22`, `max_symbols=500`: default `-1.3199% / 25.63% / DD -7.1669%`; day gate `-1.4288% / 25.56% / DD -7.2347%`. Removed rows averaged `-0.3361%`, added rows averaged `-2.8664%`, so it degraded the pool. | Rejected for promotion. Keep default-off for audit only; future day-level context must be more selective and avoid displacing right-tail winners. |
| sell/profit and weak-bucket matrix 2026-06-22 | Default-off `enable_pure_loss_weak_bucket_penalty`, `enable_mid_profit_giveback_stop`, `enable_dynamic_failed_launch_exit_stop`, and momentum/profit combinations. | After market-context consistency fix, default was about `-1.37% / 24.76% / DD -7.08%`. Pure weak-bucket alone worsened to about `-1.50% / 24.56% / DD -7.20%`; mid-profit only improved to about `-1.21% / 26.25% / DD -6.93%`; dynamic-failed + mid-profit improved to about `-1.17% / 25.99% / DD -6.86%`. `momentum_risk_control_pure_loss_plus_mfe8_giveback` was near breakeven (`about -0.04% / 29.52%`) but had worse average/worst drawdown. Narrowed `tail_momentum_hard_pure_loss_plus_mfe8_giveback` restored worst DD to default but average DD remained worse. | Keep all default-off. Profit protection is useful but insufficient alone; mainline momentum needs sharper drawdown control before any promotion. |
| `mfe8_keep4_giveback6` follow-up 2026-06-22 | Loosen mid-profit protection to sell after MFE `>=8%`, current gain `<=4%`, giveback `>=6%`, alone and with momentum/pure-loss/hard-filter combos. | Alone improved default from `-1.3148% / 25.69% / DD -7.1613%` to `-1.1368% / 27.55% / DD -6.9664%`. The best return/win combo reached `+0.0371% / 30.71%`, but average/worst DD worsened to `-7.8020% / -35.3124%`; hard-filter combo kept worst DD at `-34.1282%` but average DD remained worse (`-7.4194%`). | Test-channel only. Useful sell tweak, not promotable until average drawdown is controlled. |
| trend trailing buffer 2026-06-28 | Parameterized `trend_trailing_stop` drawdown thresholds with `trend_trailing_dd_buffer_pct`, default `0.0`. | Full in-sample scan peaked near buffer `0.06`, but CPCV on 3000 symbols returned PBO `1.0`; baseline buffer `0` was best on every OOS path. | Keep default `0.0`; parameter is research-only and must not be enabled for the product baseline without new OOS evidence. |
| `#214..#217` | Support-stop delay, peak giveback, low-suction false-launch watch, missed-candidate quality rotation. | All materially below baseline. | Rejected as default rules. |
| `#195..#201/#207/#208/#211` | Mid-profit giveback, low-suction launch gates, repeated dragon, launch quality/risk score, failed-launch exit variants. | Some reduce local loss buckets, none improve full portfolio return over baseline. | Keep as audit context only. |
| `#175/#177/#190/#194` | Low-suction/dragon boundary and current `0.1.21` refresh lineage. | Current lineage fixed several single-stock interpretation issues and reached the current baseline. | Useful code evidence, but not proof of stable alpha across regimes. |

## Read-Only Matrices Kept In Product UI

- Candidate independent trade quality report: judges candidate quality apart from cash, max positions, full portfolio, existing holdings, and replacement. Use it to separate “candidate is good/bad” from “portfolio execution path is good/bad”; read both cumulative daily TopN and individual signal-day Top10/Top20 tables.
- Performance attribution report: compares a current run with a historical comparable run and shows parameter equality, candidate schema lineage, exit-reason contribution, missing old winners, and newly added losers. Use it first when asking why收益/胜率 changed.
- Market phase audit: use it to compare `主升 / 震荡 / 退潮 / 回暖`, not to directly weight trades.
- Phase × setup-family matrix: explains low-suction first lift, dragon pullback, buildup, and overlap buckets.
- Replacement-quality matrix: explains whether freed slots buy weak follow-up candidates.
- Rotation opportunity and trend-winner protection matrices: explain why naive rotation harms existing winners.
- Stock detail market line and unified markers: visual audit layer only; the bull/bear line does not change strategy rules.

## Verification Entrypoints

- Main backend test file: `uv run pytest tests/alphaagent/test_quant_backtest_portfolio.py -q`.
- Compile check: `uv run python -m compileall alphaagent/server/api alphaagent/server/services`.
- Candidate quality matrix report: `DATABASE_URL=... ALPHAAGENT_RUN_STRATEGY_MATRIX_REPORT=1 uv run pytest tests/alphaagent/test_quant_strategy_acceptance.py::test_current_strategy_candidate_quality_matrix_report -q -s`. Quick mode compares default, low-suction buildup, tail risk, momentum, momentum risk-control, momentum hard-filter, surge-quality, Top20 day-quality, weekly-relief, high-risk D+2 follow-through, weak-bucket, profit-protection, and selected combined variants including `tail_momentum_hard_pure_loss_plus_mfe8_giveback`. With market-context consistency fixed, `max_symbols=500` took about `1:06` to `3:30` locally on 2026-06-22 depending on cache warmth; add `ALPHAAGENT_STRATEGY_MATRIX_FULL=1` only after the full matrix path has a practical cache/progress solution.
- Candidate factor path report: `DATABASE_URL=... ALPHAAGENT_RUN_CANDIDATE_FACTOR_PATH_REPORT=1 ALPHAAGENT_QUICK_ACCEPTANCE_MAX_SYMBOLS=500 uv run pytest tests/alphaagent/test_quant_strategy_acceptance.py::test_current_strategy_candidate_factor_path_report -q -s`. This explains current Top20 surge/decline/pure-loss/giveback buckets and is read-only.
- Candidate launch path report: `DATABASE_URL=... ALPHAAGENT_RUN_CANDIDATE_LAUNCH_PATH_REPORT=1 ALPHAAGENT_QUICK_ACCEPTANCE_MAX_SYMBOLS=500 uv run pytest tests/alphaagent/test_quant_strategy_acceptance.py::test_current_strategy_candidate_launch_path_report -q -s`. This explains when current Top20 first lifts hard or first breaks down after D+1 entry, and is read-only.
- Top80 launch-quality postprocess report: `DATABASE_URL=... ALPHAAGENT_RUN_STRATEGY_POSTPROCESS_REPORT=1 ALPHAAGENT_QUICK_ACCEPTANCE_MAX_SYMBOLS=500 uv run pytest tests/alphaagent/test_quant_strategy_acceptance.py::test_current_strategy_candidate_quality_postprocess_report -q -s`. This compares default, V2/V3/V4/V5 postprocess variants, profit protection, replacement quality, and clean metrics excluding suspected price discontinuities; all variants are read-only and default-off.
- Postprocess stock audit report: `DATABASE_URL=... ALPHAAGENT_RUN_POSTPROCESS_STOCK_AUDIT=1 ALPHAAGENT_QUICK_ACCEPTANCE_MAX_SYMBOLS=500 ALPHAAGENT_STOCK_DRILLDOWN_SAMPLE_LIMIT=5 uv run pytest tests/alphaagent/test_quant_strategy_acceptance.py::test_current_strategy_postprocess_stock_audit_report -q -s`. This drills into V2/V5 removals/additions with signal-day factors, funding proxies, MA/pressure, market context and entry path; read-only.
- Quick candidate acceptance with DB: `DATABASE_URL=... ALPHAAGENT_QUICK_ACCEPTANCE_MAX_SYMBOLS=500 uv run pytest tests/alphaagent/test_quant_strategy_acceptance.py::test_current_strategy_quick_acceptance_uses_latest_data_window -q` passed on 2026-06-22 in `52.21s`.
- Frontend build: `pnpm --dir frontend run build`.
- Useful APIs:
  - `GET /api/backtests?run_type=portfolio&strategy=mainline_dragon_pullback&baseline_only=true&limit=5`
  - `GET /api/backtests/{id}/performance-attribution`
  - `GET /api/backtests/{id}/phase-strategy-family-matrix`
  - `GET /api/backtests/{id}/candidate-trade-quality-report`
  - `GET /api/backtests/{id}/replacement-quality-matrix`
  - `GET /api/backtests/{id}/support-stop-matrix`
  - `GET /api/backtests/{id}/rotation-opportunity-cost-matrix`
  - `GET /api/backtests/{id}/trend-winner-protection-matrix`
  - `GET /api/quant/symbols/{vt_symbol}/signal-history`
  - `GET /api/quant/symbols/{vt_symbol}/market-line`

## Open Risks

- Current baseline still overlaps a strong 2025-2026 technology/speculation window; it is not enough to claim stable alpha.
- Long historical sector/fund-flow coverage remains insufficient for robust mainline rotation.
- Weak/bear-market and non-technology regime validation still requires broader walk-forward and parameter-sensitivity checks.
- `#275` is the fresh no-cache analysis reference for future `max_positions` / `candidate_limit` sensitivity grids.
- Any future default rule must beat current product default `#203/#194` on both return and win rate, then explain whether it recovers the historical winner contribution without relying on stale candidate inputs.
- The current matrix path still recomputes daily symbol scores and market context heavily. Expanded quick `max_symbols=500` is usable but slow when risk-control variants require market context; full `max_symbols=5000` was manually interrupted after `2:18`. Add candidate scoring cache/progress before relying on it for repeated full-market experiment sweeps. The next infrastructure improvement should be a reusable candidate-score snapshot for the unified test channel so factor experiments can rerank/simulate without repeated all-symbol rescoring.
