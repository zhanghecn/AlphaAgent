# 正式与本地打板回测一致性审计

## 当前状态（2026-07-16）

- 正式 API 已部署 `v2.5.20-exit1430.20260716`；没有复制本地 PostgreSQL，也没有
  使用 CSV、模板、用户文件或服务器文件导入数据。
- 首次全量自举批次 `6ffd259cc4744bdc87e704221395b825` 完成 4,573,456 行后，
  又从正式页面提前触发 `eod_finalize_2130`；复验批次
  `fb361830c02f4eb384e8467c5788d30e` 为 `succeeded`：9/9 成功、0 失败，读取和
  写入均为 73,301 行，耗时 `16:57:56..17:29:33`。
- 复验链路自主写入日线 23,493 根、板块评分 994 行、事件分钟 200/200（48,000 根）、
  候选 D+1 14:30 12/12（12 根），随后重建 800 日历史账本和 2 个 final 候选。
  重建新发现的 4 个可重试退出价又通过页面单任务 `run_id=560` 完成 4/4，并再次重建。
- 最终持久化候选池有 213 个 D+1 请求，精确 14:30 已覆盖 122 个；剩余 91 个旧日期
  都因 TDX 局部翻页错误处于 1 天冷却，当前可重试数为 0。
- 正式质量接口为 `limit-up-history-v15`、`research_ledger_ready=true`；历史账本范围
  `2023-03-28..2026-07-16` 共 800 日。`simulation_eligible=false` 继续保持，因为逐日
  成员、严格竞价、分钟覆盖、板块分钟流、Tick/L2 和真实前向观察仍未齐。
- 页面复验显示批次 9/9 成功；API、Gateway、PostgreSQL、Redis 均健康且 API 容器
  `restart=0`。重建高负载期间浏览器轮询曾出现 5 次短暂 502，随后连续恢复 200；
  API/Gateway 容器日志没有 ERROR、Traceback 或记录到的 5xx。
- 同步成功不等于回测已经与本地收敛。共同 603 日区间仍因首板盘中路径和财报覆盖不足
  少 122 个组合信号，并回退到仅首板组合；详细数字见下节。

## 发布后正式/本地回测对比（2026-07-16）

两边均使用 `limit-up-history-v15 / limit-up-scheduled-v4 / limit-up-cash-v4`，并固定
共同区间 `2024-01-15..2026-07-15`、`lane=portfolio`，排除正式多出的 2026-07-16
和 800/603 日区间差异：

| 共同区间口径 | 入选组合 | 信号 | 闭合交易 | 胜率 | 复利 | 最大回撤 | 利润因子 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 本地 | 首板+二进三 | 307 | 149 | 63.0872% | +266.4491% | -8.0275% | 2.4961 |
| 正式 | 仅首板 | 185 | 108 | 62.9630% | +142.0463% | -8.5278% | 2.4434 |

正式仍少 122 个信号、41 笔闭合交易，复利少 124.4028 个百分点。新增真实分钟路径
替换了部分更乐观的日线代理，因此收益下降不是同步失败，而是执行价格变得更保守。

同战法拆分后：

- 首板：本地 `290/139/+224.0076%/-7.9408%`，正式
  `185/108/+142.0463%/-8.5278%`（信号/闭合/复利/回撤）。
- 二进三：本地 `17/17/+27.7336%/-4.7516%`，正式
  `16/16/+23.8855%/-4.7516%`，已经基本一致。
- 正式强制计算“首板+二进三”可得到 201 个信号、120 笔交易、`+181.9210%`，但
  回撤 `-11.8265%`、双倍成本回撤 `-12.7324%`，未通过回撤门，因此系统正确回退
  到仅首板。相同组合在本地回撤 `-8.0275%`、双倍成本回撤 `-8.6789%`，全部门禁通过。

剩余差异主要来自首板路径输入尚未自主收敛：

| 输入 | 本地 | 正式 |
| --- | ---: | ---: |
| 事件分钟/预览路径 | 20,176 | 18,971 |
| 分钟 fallback 路径 | 2,343 | 853 |
| 首板正式调度信号 | 290 | 185 |
| 财报覆盖股票 | 1,524 | 893 |
| 逐日行业成员覆盖日 | 3 | 1 |
| 选中信号精确 14:30 | 98 | 117 |

候选退出价数量已追上本地，证明原结构缺口已修复；组合仍不一致，说明退出价不是剩余
差异的主因。正式分钟/预览路径仍少 1,205 对，分钟 fallback 少 1,490 对，财报覆盖少
631 只。后续调度必须继续补分钟路径、财报和逐日成员，并在每次输入增加后重建账本；
未收敛前不能宣称正式回测已与本地一致。

当前自动收敛边界：

- `sync_limit_up_event_minutes` 只按事件股票的信号日补 `09:15..15:00`，每个 21:30
  批次最多 200 对。本轮 200/200 成功，但正式分钟 fallback 仍比本地少 1,490 对，
  还需多轮调度，实际速度受退避和供应商空响应影响。
- 19:00 包含季度财报和财务指标任务，默认各处理 100 只缺失股票；正式比本地少覆盖
  631 只，可继续自动追赶，随后 21:30 账本重建应吸收新财报。
- `sync_limit_up_exit_minutes` 从持久化研究候选池派生 `result_date`，只下载精确 14:30，
  使用 `tdx_exit_1430` 独立退避。连续正式触发写入 `103 + 12 + 4 = 119` 根，当前
  `122/213` 已覆盖、91 个冷却、0 个可立即重试；旧日期受 TDX 回溯边界限制时保留
  缺口，不使用日线或 CSV 冒充同步覆盖。
- 同花顺公开源只有滚动近 252 日。正式事件覆盖 255 日、本地 258 日的较早差异，以及
  500 日事件目标，不能仅靠当前 THS 任务补齐；需要系统侧更长历史供应商。
- 逐日行业成员、严格竞价、板块分钟流和 Tick/L2 仍是模拟资格门禁，不是本地回测
  数字对齐的前置条件；当前供应商/调度也无法把这些历史门禁快速回补到 500/60 日。

## 发布前根因结论

以下数字是修复发布前的审计基线，用于解释原始巨大差异，不代表当前正式状态。

- 正式环境运行 `v2.5.18`，镜像 revision 为
  `e4684658238ca003b95c7f5319b4ba3f52781a56`，对应
  `limit-up-history-v14 / limit-up-live-v4 / limit-up-scheduled-v3`。
- 正式回测只有 13 个信号、2 笔闭合交易，胜率 50%，账户收益
  `-0.6152%`。正式 `/data-quality` 同时明确返回
  `research_ledger_ready=false` 和 `simulation_eligible=false`。
- 本地用同一个 `v2.5.18` 镜像、同一个 `2025-06-10..2026-07-15`
  选择区间读取本地完整证据，仍得到 290 个信号、139 笔闭合交易、
  胜率 `62.5899%`、收益 `+224.0076%`、最大回撤 `-7.9408%`。
  因此 603 日与 268 日的显示范围不是本次巨大差异的直接主因。
- 直接主因是正式库只有 18 个涨停事件交易日、1,870 条可用事件，
  本地有 258 个事件交易日、20,910 条事件。正式环境没有执行同花顺
  近 252 日一次性历史证据回补，夜间调度也不包含这项回补。
- 本地当前工作区还有未发布的 v15/v6/v4 改动。完整本地库下，v15
  综合首板与二进三为 307 个信号、149 笔交易、收益 `+266.4491%`。
  这部分相对已发布 v14 增加 `42.4415` 个百分点，但不能解释正式环境
  从 `+224.0076%` 变成 `-0.6152%` 的主体差异。

## 对照矩阵

| 运行口径 | 事件交易日 | 信号 | 闭合交易 | 胜率 | 账户收益 | 最大回撤 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 正式 v2.5.18 + 正式库 | 18 | 13 | 2 | 50.0000% | -0.6152% | -0.6152% |
| 正式 v2.5.18 + 本地库，相同 268 日区间 | 258 | 290 | 139 | 62.5899% | +224.0076% | -7.9408% |
| 本地未发布 v15 + 本地库 | 258 | 307 | 149 | 63.0872% | +266.4491% | -8.0275% |

正式与本地其他关键覆盖差异：

| 数据 | 正式 | 本地 |
| --- | ---: | ---: |
| 可靠日线账本 | 268 日 | 603 日 |
| 涨停事件记录 | 1,870 条 | 20,910 条 |
| 事件分钟/预览路径 | 305 条 | 20,176 条 |
| 财报覆盖股票 | 893 只 | 1,524 只 |
| 历史账本版本 | v14，268 行 | v14/v15 均为 603 行 |

## 根因链路

1. `sync_stock_daily_bars` 的正式任务默认参数仍是 `limit=250`。数据健康
   只检查是否更新到最新交易日，不检查纵向回测深度，所以页面显示日线
   “新鲜”，但历史账本仅完成目标 500 日的 `53.6%`。
2. 正式调度只有实时涨停池和 `sync_limit_up_event_minutes`。后者只能给已存在
   的事件补分钟线，不能创造缺失的 252 日历史事件。
3. `sync_limit_up_ths_evidence` 只由数据页后台批次触发，不在 `eod_1900` 或
   `eod_finalize_2130` 中。正式库因此只保留近期东方财富事件，事件门禁仅
   `18/500 = 3.6%`。
4. `/limit-up` 回测页仍展示部分账本计算出的账户指标，只以“研究账户”弱提示，
   没有在 `research_ledger_ready=false` 时隐藏或阻断主指标，放大了误解。
5. 正式服务器和容器均健康；审计时 API 约占 706 MiB 内存、CPU 约 2%，
   PostgreSQL 约占 230 MiB，未发现回测接口错误。8 核 16 GiB 不是本次根因。

## 修复实现

现已发布的实现改为自主收敛，不需要也不应从本地复制 PostgreSQL：

1. 全市场增量日线发现可靠历史少于 750 日时，自动改为每股最多 800 根的
   有界历史回补；达到目标后恢复最近窗口增量。定向股票同步不触发全市场自举。
2. `eod_finalize_2130` 按“日线 -> 同花顺 252 日事件 -> 事件分钟 -> 候选 D+1
   14:30 -> 账本重建”串行执行。同花顺任务只补缺失日，覆盖不足或空响应不覆盖旧证据。
3. 同批次历史输入有新增时强制重建；即使进程在回补后、重建前退出，后续调度
   也会比较账本与日线、分钟、事件、财报、股票和成员表的持久化更新时间并续跑。
4. `GET /api/data-sync/health` 暴露可靠历史起止、天数、750 日目标和 readiness；
   最新日期新鲜但纵向不足时仍推荐同步，不能再把 268 日账本当成完整状态。

正式部署前已生成并校验 PostgreSQL 归档
`/opt/1panel/project/AlphaAgent/backups/pre-autosync-20260716T0945Z.dump`；正式批次证明
上述链路可从系统供应商自行收敛。Tick/L2、竞价、逐日成员和前向证据未补齐前，
`simulation_eligible` 必须继续为 `false`，不能把部分研究账本作为可模拟交易结论。

本地验收（2026-07-16）：聚焦回归 `178 passed`，compileall、致命 Ruff 规则和
`git diff --check` 通过。真实本地 PostgreSQL 从候选池派生 319 个请求，已有精确
14:30 共 98 个；新任务 dry-run 从数据库自动选出缺口并由 TDX 取回精确单分钟，
`rows_written=0`。正式发布后同一服务端路径连续实际写入 119 根，没有复制数据库。

## 验证边界

- 发布前根因审计阶段只读；随后在完整备份后部署新镜像并手动触发正式调度，没有复制、
  导入或替换数据库。
- 历史结果仍大量使用收盘价代理，且 Tick/L2、竞价、逐日概念成员和 60 日
  真实前向证据未齐，`simulation_eligible` 继续为 `false`。
