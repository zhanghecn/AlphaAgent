# 反包 · 研究方向与执行计划

（配套 `反包需求.md` + `反包规则.md`。）

## 一、版图位置

| 研究 | 断板时长 | 结构 |
|---|---|---|
| 高位接力 | 0 天（不断板） | 连板直接冲下一板 |
| **反包（本研究）** | **1~3 天** | 短断板快速再涨停 |
| N 型补涨打板 | 坑宽 6~15 天 | 深坑走完再启动首板 |

两个现有研究的触发池都收不到断板 1~3 天的样本（高位接力要求昨日仍涨停；N 型坑宽
要求 6 天+），反包是空白带；字段口径、验证方法全部继承两个库。

## 二、阶段与进度（2026-09-25）

- **Phase 0 全量基础矩阵 ✅**：7978 笔事件（主格 1624），18 格总表 + 参考行 + 分年，
  三道验证全过（两库交叉对账 + 门禁 68/68，见 `汇总/对账记录.md`）。
- **Phase 1 条件挖掘 ✅**：2板冠军格（`汇总/2板反包最佳形式.md`）+ 4+ 多维立方体
  （`汇总/4+多维立方体.md`）→ 六组框架 + S1/S2/S3 方案点定稿（`反包规则.md`），
  好差票验证月度文件已带方案点标记。
- **Phase 2 稳健性（待做）**：五道检验（搜索级零假设 / 单格置换 / bootstrap /
  去最好 3 笔 / 留一年）+ 案例门禁抽样重算 + 问财条件句。
- **Phase 3 产品化 ✅（2026-09-25）**：短线研究第 5 页签 `/short-term?research=fanbao`
  上线（fbb-v2.0）。后端 `services/fanbao/` 七文件 + `fbb_` 五表 + 调度四档
  （盘中每分钟 / eod 19:00·21:30 批 / 回测 23:20）+ worker reconcile；
  前端 `FanbaoPage` 四页签（实时推荐 / 回测 18 格矩阵 / 交割单好差票分组 / 规则说明）。
  产品回测锚点对账全过：方案点 7/7（n_diff 全 0）、矩阵抽格 3/3、案例门禁 8/8。

## 三、数据与代码

- 日线：postgres `stock_daily_bars`，容器内跑脚本，输出 `/tmp/research/fanbao_out`
  后回宿主。连板段计算复用高位接力 `relay_research.py` 的 streak 分段逻辑。
- 分钟（首刻）：通达信 15mK，复用 `relay_touch_fetch.py`，存档约 2024-08-15 起。
- 脚本（`scripts/`）：
  - `fanbao_research.py`（容器）：事件构建 + 六组矩阵 + 方案点 + 月度文件；
  - `fanbao_reconcile.py`（宿主机）：两库交叉对账；
  - `fanbao_casecheck.py`（容器）：案例门禁独立重算；
  - `fanbao_cube.py`（宿主机）：4+ 多维立方体；
  - `fanbao_2board.py`（宿主机）：2板最佳形式；
  - `fanbao_ma5.py`（容器）：距 5 日线验证。
- ⚠️ 回拷教训：`rsync -a --delete` 源目录放在目标目录内部会自吞噬（曾误删 scripts/
  与文档）。回拷一律 `docker cp` 到目录外 + `cp` 覆盖，不用 `rsync --delete`。
