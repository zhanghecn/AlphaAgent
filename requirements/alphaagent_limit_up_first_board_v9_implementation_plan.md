# 首板 v9 两阶段修复策略实施计划

**目标：** 将已复核的高低胜率首板因子落成独立、可审计、默认不执行的 v9 产品策略。

**架构：** `lane_research.py` 保持纯函数规则边界；历史和实时分别负责提供点时财报、情绪与盘中字段；现有交割单和回测接口消费相同的候选结果。v8 数据保留在数据库中，v9 使用新版本全量重建。

## 任务 1：首板纯规则

- [x] 在 `tests/alphaagent/test_limit_up_lanes.py` 增加触板次数、点时利润增长、D-1 分歧和高位局部承接测试。
- [x] 运行目标测试并确认旧实现失败。
- [x] 修改 `alphaagent/server/services/limit_up/lane_research.py`，移除 4%-6%区间和无效质量线，增加两阶段字段与阻断码。
- [x] 运行分战法测试并确认接力板行为未改变。

## 任务 2：实时点时字段与修复门

- [x] 在 `tests/alphaagent/test_limit_up_live.py` 增加财报快照传递、退潮修复允许和未修复阻断测试。
- [x] 修改 `live_repository.py` 和 `live_service.py`，传递信号日前最近财报并保留半年基因字段。
- [x] 修改 `live_policy.py`，只在实时封板增加、炸板下降、炸板率不高于35%时放行 D-1 退潮。
- [x] 运行实时测试并确认缺字段继续 fail-closed。

## 任务 3：版本、历史账本与模型候选池

- [x] 将历史、实时和模型版本分别升级为 v9/v2/v5。
- [x] 更新首板模型候选池，只把新策略选择门作为可训练对照，不放入其他风险硬门失败样本。
- [x] 运行历史、模型和数据质量测试。
- [x] 全量重建 `limit-up-history-v9`，核对逐日首只选择、阶段统计和无未来数据泄漏。

## 任务 4：精简产品呈现

- [x] 更新 `LimitUpPage.tsx` 的因子中文映射和实时修复状态文案，不新增页面或嵌套卡片。
- [x] 更新 TypeScript 类型以暴露修复确认和两阶段分数。
- [x] 运行前端测试与生产构建。

## 任务 5：部署与验收

- [x] 运行全部打板后端测试。
- [x] 构建并重启 API/Web 容器。
- [x] 在桌面和手机视口验证 `/limit-up` 的实时、日期交割单和回测；检查控制台、横向溢出和接口版本。
- [x] 更新 `memory/06_backtests/`、`memory/05_runtime/` 和决策索引的当前事实。
